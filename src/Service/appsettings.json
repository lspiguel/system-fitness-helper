{
  "//": "ConfigPath defaults to %ProgramData%\\SystemFitnessHelper\\rules.json, resolved in code. Set it here only to override, and use an absolute path or one containing %VAR% placeholders; SFH_CONFIG_PATH takes precedence over this file.",
  "ServiceConfig": {
  }
}
